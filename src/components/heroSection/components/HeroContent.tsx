import {
  HeroHeadLine,
  HeroLocationIndicator,
  HeroSearchForm,
} from './heroContent/';

const HeroContent = () => {
  return (
    <>
      <HeroHeadLine />
      <HeroSearchForm />
      <HeroLocationIndicator />
    </>
  );
};

export default HeroContent;
